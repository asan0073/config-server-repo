server:
  port: 0
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employee_db
    username: root
    password: root1234
  jpa:
    properties:
      hibernate:
        show_sql: true
        hbm2ddl:
          auto: create
  cloud:
    config:
      import-check:
        enabled: false

eureka:
  service-url:
    defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost

